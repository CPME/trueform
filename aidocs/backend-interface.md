# Backend Interface (v1)

The canonical backend interface and contract now live in the technical spec.

See `aidocs/spec.md` for:
- Backend boundary and interface definition
- Selector metadata requirements
- V1 scope reminders
